# Aula 07 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir os quatro artefatos da Aula 07 (04/09/2026), migrando o case para base mensal do SIDRA e organizando o encontro em torno de quatro hipóteses declaradas, com árvore de decisão como técnica central e KNN como pincelada.

**Architecture:** Os dados mensais entram em `dados/mensal/` sem tocar em `dados/`, que as Aulas 01 a 06 continuam lendo. Toda medição citada em slide nasce em `tools/tests/test_modelos_aula07.py`, que trava vereditos e não só números. As figuras saem de `tools/graficos_aula07.py`. Deck, material, referências e notebook consomem esses números, nunca os recalculam de memória.

**Tech Stack:** Python 3 (csv, numpy, scikit-learn, matplotlib, pandas no notebook), HTML estático com Reveal.js 5.1.0, pytest, Playwright para os validadores.

**Spec:** `docs/superpowers/specs/2026-09-03-aula07-hipoteses-arvores-e-base-mensal-design.md`

## Global Constraints

- Português do Brasil com acentuação completa em todo texto visível.
- **Travessão em dash (U+2014) é proibido** em qualquer arquivo. Usar dois-pontos, vírgula, parênteses ou hífen.
- **Emoji é proibido.** Iconografia é Google Material Symbols.
- Nenhuma cor literal e nenhum `font-family` fora de `assets/css/inteli-brand.css`. `tools/check_brand.py` reprova.
- Slides são fixos em 1280x720 e não rolam. `tools/check_slides.py` reprova estouro. Folga vertical menor que 80px no macOS é candidata a reprovar no Ubuntu do CI.
- Títulos de slide analítico são a conclusão completa com o número dentro. Títulos de slide de referência são descritivos e curtos. **Proibido**: paralelismo negativo ("é X, não Y"), antítese simétrica, escalada com dois-pontos e título de listicle contado ("As quatro hipóteses da aula").
- Grade de cartões é último recurso. Preferir tabela, depois lista numerada, depois prosa curta.
- Peso de ART citado de `PLANO_DE_ENSINO.md`. Autoestudo citado de `docs/autoestudos-por-semana.md` com título exato.
- Dado sintético é proibido. Todo número ancora em `dados/mensal/`.
- Commits em Conventional Commits.

**Números canônicos da aula** (todos medidos, nenhum recalculado de memória):

| medida | valor |
|---|---|
| séries mensais | 351 linhas (471 em ovos), 1997-01 a 2026-03 |
| base analítica | 339 linhas, 1998-01 a 2026-03 |
| treino / teste | 315 meses (1998-01 a 2024-03) / 24 meses (2024-04 a 2026-03) |
| fator da baseline C | 1,05299 |
| baseline A (repete mês anterior) | RMSE 88.925.206 / MAPE 6,73% |
| baseline B (mesmo mês do ano anterior) | RMSE 75.627.562 / MAPE 5,10% |
| baseline C (coeficiente fixo da LDC) | RMSE 54.523.588 / MAPE 3,71% |
| regressão linear, nível | RMSE 63.225.293 / MAPE 4,25% |
| árvore `max_depth=3`, nível | RMSE 109.534.336 / MAPE 7,66% |
| random forest 300, nível | RMSE 82.070.325 / MAPE 5,35% |
| KNN k=5 padronizado, nível | RMSE 128.029.380 / MAPE 9,37% |
| KNN k=5 sem padronizar, nível | RMSE 83.496.528 / MAPE 5,62% |
| máximo do alvo no treino | 1.226.709.256 (2023-03) |
| teto da árvore (maior previsão) | 1.090.166.234 |
| máximo real no teste | 1.301.022.625 (2026-03) |
| meses de teste acima do teto | 23 de 24 |
| meses de teste acima do máximo do treino | 5 de 24 |
| folhas da árvore `max_depth=3` | 8 |
| participação na distância sem padronizar | `lag1` 49,2775%, `lag12` 50,7225%, `sen` 0,0000%, `cos` 0,0000% |
| KNN padronizado x sem padronizar | k=3: 8,14% x 5,82%; k=5: 9,37% x 5,62%; k=10: 10,09% x 6,10% |
| árvore, alvo em razão | RMSE 59.186.695 / MAPE 3,86% |
| random forest, alvo em razão | RMSE 70.292.218 / MAPE 4,48% |
| KNN k=5 padronizado, razão | RMSE 69.920.366 / MAPE 4,59% |
| KNN k=5 sem padronizar, razão | RMSE 56.355.917 / MAPE 3,71% |
| regressão linear, razão, 4 features | RMSE 71.970.145 / MAPE 4,73% |
| modelo do fecho (11 features, razão) | RMSE 46.724.434 / MAPE 3,32% |

**Definição única da base analítica**, usada identicamente em teste, figura e notebook:

- Junção interna das cinco séries de `dados/mensal/` pela coluna `periodo`, ordenada.
- `mes` = inteiro dos dois últimos caracteres de `periodo`; `dias` = dias do mês civil.
- `sen = sin(2*pi*mes/12)`, `cos = cos(2*pi*mes/12)`.
- `lag1`, `lag2`, `lag3`, `lag12` = `abate_frangos` deslocado em 1, 2, 3 e 12 meses.
- `<serie>_lag1` para as outras quatro séries, deslocadas em 1 mês.
- Descartar linhas com qualquer valor ausente. Resultado: 339 linhas.
- `FEATURES = ["lag1", "lag12", "sen", "cos"]`, `ALVO = "abate_frangos"`, `N_TESTE = 24`.
- Todo modelo com aleatoriedade usa `random_state=42`.

---

## File Structure

| arquivo | responsabilidade |
|---|---|
| `tools/baixar_dados.py` | baixa trimestral (como hoje) e mensal (novo), sem quebrar o contrato antigo |
| `dados/mensal/*.csv` | as cinco séries mensais versionadas |
| `dados/README.md` | contrato mensal, correção da afirmação errada, nota de arredondamento |
| `tools/tests/test_dados_mensal.py` | contrato dos CSVs mensais e reconciliação com o trimestral |
| `tools/tests/test_modelos_aula07.py` | trava os vereditos das quatro hipóteses |
| `tools/graficos_aula07.py` | as duas figuras da aula |
| `notebooks/aula07.ipynb` | as quatro hipóteses executadas pela dupla |
| `aulas/aula07.html` | deck, cerca de 30 slides |
| `materiais/aula07.html` | material de apoio |
| `referencias/aula07.html` | os doze autoestudos restantes da Semana 05 |
| `docs/notas-do-professor/aula07.md` | condução, ordem de corte, resposta sobre o autoestudo |
| `docs/adrs/ADR-010-base-mensal-e-escopo-da-aula-07.md` | revisão da ADR-003 e corte de escopo |
| `index.html`, `PLANEJAMENTO_AULA_A_AULA.md`, `PLANO_DE_ENSINO.md`, `docs/ANDAMENTO.md` | amarração |

---

### Task 1: Base mensal do SIDRA

**Files:**
- Modify: `tools/baixar_dados.py`
- Create: `dados/mensal/abate_bovinos.csv`, `dados/mensal/abate_suinos.csv`, `dados/mensal/abate_frangos.csv`, `dados/mensal/producao_ovos.csv`, `dados/mensal/producao_leite.csv`
- Create: `tools/tests/test_dados_mensal.py`
- Modify: `dados/README.md`

**Interfaces:**
- Consumes: nada.
- Produces: `dados/mensal/<serie>.csv` com colunas `periodo` (`AAAA-MM`), `valor` (float ou vazio) e `unidade`. `baixar_dados.normalizar_periodo_mensal(trimestre_codigo, referencia_codigo) -> str`, que recebe `"202601"` e `"115233"` e devolve `"2026-01"`.

- [ ] **Step 1: Escrever o teste que falha**

Criar `tools/tests/test_dados_mensal.py`:

```python
"""Trava o contrato dos CSVs mensais e a reconciliacao com os trimestrais.

A base mensal existe porque as cinco tabelas do SIDRA trazem a classificacao
c12716 (Referencia temporal), com "No 1o mes", "No 2o mes" e "No 3o mes" alem
de "Total do trimestre". A ADR-010 registra a decisao; este arquivo trava o
resultado.

A reconciliacao e o teste que importa: se a soma dos tres meses nao devolver o
trimestre ja versionado em dados/, o mapeamento de mes esta errado e a serie
mensal esta silenciosamente deslocada no tempo.
"""
import csv
import os
import re
import sys

import pytest

RAIZ = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
DADOS = os.path.join(RAIZ, "dados")
MENSAL = os.path.join(DADOS, "mensal")

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import baixar_dados  # noqa: E402

ESPERADOS = [
    "abate_bovinos.csv", "abate_suinos.csv", "abate_frangos.csv",
    "producao_ovos.csv", "producao_leite.csv",
]

# Ovos e leite vem em "Mil duzias" e "Mil litros". O IBGE arredonda cada mes
# de forma independente, entao a soma de tres valores arredondados nao precisa
# igualar o total trimestral arredondado. A divergencia observada e de 1
# unidade, uma parte em um milhao. As tres series de abate, em quilogramas,
# reconciliam sem nenhuma divergencia.
TOLERANCIA = {
    "abate_bovinos.csv": 0,
    "abate_suinos.csv": 0,
    "abate_frangos.csv": 0,
    "producao_ovos.csv": 1,
    "producao_leite.csv": 1,
}


def _ler(caminho):
    with open(caminho, encoding="utf-8") as fh:
        return list(csv.DictReader(fh))


def test_todos_os_csv_mensais_existem():
    for nome in ESPERADOS:
        assert os.path.isfile(os.path.join(MENSAL, nome)), nome


def test_colunas_do_contrato():
    for nome in ESPERADOS:
        linhas = _ler(os.path.join(MENSAL, nome))
        assert set(linhas[0]) == {"periodo", "valor", "unidade"}, nome


def test_periodo_em_ano_mes_ordenado_e_sem_buraco():
    for nome in ESPERADOS:
        periodos = [l["periodo"] for l in _ler(os.path.join(MENSAL, nome))]
        assert periodos == sorted(periodos), nome
        assert len(periodos) == len(set(periodos)), nome
        for p in periodos:
            assert re.fullmatch(r"\d{4}-(0[1-9]|1[0-2])", p), (nome, p)
        # sem buraco: a contagem tem de bater com a distancia entre extremos
        def indice(p):
            ano, mes = p.split("-")
            return int(ano) * 12 + int(mes)
        assert indice(periodos[-1]) - indice(periodos[0]) + 1 == len(periodos), nome


def test_as_tres_series_de_abate_tem_351_linhas():
    for nome in ["abate_bovinos.csv", "abate_suinos.csv", "abate_frangos.csv"]:
        linhas = _ler(os.path.join(MENSAL, nome))
        assert len(linhas) == 351, nome
        assert linhas[0]["periodo"] == "1997-01", nome
        assert linhas[-1]["periodo"] == "2026-03", nome


def test_nenhum_valor_ausente():
    for nome in ESPERADOS:
        vazios = [l["periodo"] for l in _ler(os.path.join(MENSAL, nome)) if not l["valor"]]
        assert vazios == [], (nome, vazios[:5])


def test_soma_dos_tres_meses_reconcilia_com_o_trimestre():
    """O teste que prova que o mapeamento de mes esta certo."""
    for nome in ESPERADOS:
        trimestral = {l["periodo"]: float(l["valor"])
                      for l in _ler(os.path.join(DADOS, nome)) if l["valor"]}
        soma = {}
        for l in _ler(os.path.join(MENSAL, nome)):
            ano, mes = l["periodo"].split("-")
            chave = "%s-T%d" % (ano, (int(mes) - 1) // 3 + 1)
            soma[chave] = soma.get(chave, 0.0) + float(l["valor"])
        comuns = sorted(set(trimestral) & set(soma))
        assert len(comuns) >= 117, nome
        for chave in comuns:
            assert abs(trimestral[chave] - soma[chave]) <= TOLERANCIA[nome], \
                (nome, chave, trimestral[chave], soma[chave])


def test_as_series_de_abate_reconciliam_exatamente():
    """Separado de proposito: em quilogramas nao ha arredondamento, e uma
    divergencia de 1 kg ja seria sinal de mapeamento errado."""
    for nome in ["abate_bovinos.csv", "abate_suinos.csv", "abate_frangos.csv"]:
        trimestral = {l["periodo"]: float(l["valor"])
                      for l in _ler(os.path.join(DADOS, nome)) if l["valor"]}
        soma = {}
        for l in _ler(os.path.join(MENSAL, nome)):
            ano, mes = l["periodo"].split("-")
            chave = "%s-T%d" % (ano, (int(mes) - 1) // 3 + 1)
            soma[chave] = soma.get(chave, 0.0) + float(l["valor"])
        divergentes = [c for c in set(trimestral) & set(soma)
                       if trimestral[c] != soma[c]]
        assert divergentes == [], (nome, divergentes[:5])


def test_normalizar_periodo_mensal():
    assert baixar_dados.normalizar_periodo_mensal("202601", "115233") == "2026-01"
    assert baixar_dados.normalizar_periodo_mensal("202601", "115234") == "2026-02"
    assert baixar_dados.normalizar_periodo_mensal("202601", "115235") == "2026-03"
    assert baixar_dados.normalizar_periodo_mensal("202504", "115235") == "2025-12"
    assert baixar_dados.normalizar_periodo_mensal("202502", "115233") == "2025-04"
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `python3 -m pytest tools/tests/test_dados_mensal.py -v`
Expected: FAIL. Os testes de arquivo falham com `assert os.path.isfile(...)` e `test_normalizar_periodo_mensal` falha com `AttributeError: module 'baixar_dados' has no attribute 'normalizar_periodo_mensal'`.

- [ ] **Step 3: Estender `tools/baixar_dados.py`**

Trocar o docstring do módulo (linhas 1 a 16) por um que não afirme mais que mensal não existe:

```python
"""
Baixa do SIDRA/IBGE as series de proteina animal do case da LDC, em duas
granularidades.

Roda uma vez e versiona o resultado. Os notebooks leem do CSV, nunca da rede:
a aula nao pode depender de o IBGE estar no ar.

As tabelas sao da Pesquisa Trimestral do IBGE, e trazem a classificacao
c12716 (Referencia temporal) com quatro categorias: "Total do trimestre",
"No 1o mes", "No 2o mes" e "No 3o mes". Ou seja, a mesma tabela publica o
trimestre e os tres meses dele.

  dados/         serie trimestral, periodo AAAA-TN. Usada pelas Aulas 01 a 06.
  dados/mensal/  serie mensal, periodo AAAA-MM. Usada da Aula 07 em diante.

A versao trimestral continua sendo gerada porque quatro aulas publicadas leem
dela e citam achados medidos nela. A decisao esta em docs/adrs/ADR-010, que
revisa parcialmente a ADR-003.

Uso:
    python3 tools/baixar_dados.py
"""
```

Acrescentar, depois da constante `URL`:

```python
# Mesma URL, pedindo as tres categorias mensais da classificacao c12716
# (Referencia temporal) em vez do "Total do trimestre" que o filtro de
# e_linha_total seleciona por omissao.
URL_MENSAL = ("https://apisidra.ibge.gov.br/values/t/%s/n1/all/v/%s/p/all"
              "/c12716/115233,115234,115235")

DESTINO_MENSAL = os.path.join(DESTINO, "mensal")

# Categorias de c12716 e a posicao do mes dentro do trimestre.
POSICAO_DO_MES = {"115233": 1, "115234": 2, "115235": 3}
```

Acrescentar as funções novas, depois de `normalizar_periodo`:

```python
def normalizar_periodo_mensal(codigo_trimestre, codigo_referencia):
    """Converte trimestre do SIDRA mais posicao do mes para AAAA-MM.

    O SIDRA devolve o trimestre como AAAATT (202601 = 1o trimestre de 2026) e
    a posicao do mes como categoria de c12716 (115233 = "No 1o mes"). O mes
    civil e (trimestre - 1) * 3 + posicao, entao 202601 com 115233 vira
    2026-01, e 202504 com 115235 vira 2025-12."""
    texto = str(codigo_trimestre).strip()
    trimestre = int(texto[4:])
    mes = (trimestre - 1) * 3 + POSICAO_DO_MES[str(codigo_referencia).strip()]
    return "%s-%02d" % (texto[:4], mes)


def e_linha_total_mensal(item, chave_referencia):
    """Como e_linha_total, mas deixa passar a dimensao de referencia temporal.

    Sem essa excecao o filtro descartaria justamente as tres categorias
    mensais, porque nenhuma delas comeca com 'Total'."""
    for chave in CHAVES_DIMENSAO:
        if chave == chave_referencia:
            continue
        valor = item.get(chave)
        if valor is not None and not valor.startswith("Total"):
            return False
    return True


def extrair_linhas_mensais(payload):
    """Mesma logica de extrair_linhas, no eixo mensal.

    A chave da referencia temporal e descoberta pelo rotulo do cabecalho, e
    nao fixada em D5N, porque a tabela 1092 tem uma dimensao a mais (tipo de
    rebanho bovino) e a de ovos tem finalidade da producao: a posicao da
    coluna muda de tabela para tabela."""
    cabecalho, *dados = payload
    unidade = cabecalho.get("MN", "") or cabecalho.get("MC", "")

    chave_ref_nome = None
    for chave, rotulo in cabecalho.items():
        if rotulo.startswith("Referência temporal (Código)"):
            chave_ref_codigo = chave
            chave_ref_nome = chave[:-1] + "N"
    if chave_ref_nome is None:
        raise ValueError(
            "a tabela nao trouxe a classificacao c12716 (Referencia "
            "temporal); sem ela nao ha serie mensal para extrair")

    chave_periodo = None
    for chave, rotulo in cabecalho.items():
        if rotulo.startswith("Trimestre (Código)"):
            chave_periodo = chave
    if chave_periodo is None:
        raise ValueError("a tabela nao trouxe a coluna de trimestre")

    linhas_por_periodo = {}
    contagem = {}
    for item in dados:
        if not e_linha_total_mensal(item, chave_ref_nome):
            continue
        codigo_ref = item.get(chave_ref_codigo)
        if str(codigo_ref) not in POSICAO_DO_MES:
            continue
        periodo = normalizar_periodo_mensal(item.get(chave_periodo), codigo_ref)
        contagem[periodo] = contagem.get(periodo, 0) + 1
        linhas_por_periodo.setdefault(periodo, {
            "periodo": periodo,
            "valor": converter(item.get("V", "")),
            "unidade": item.get("MN", unidade),
        })

    duplicados = {p: n for p, n in contagem.items() if n > 1}
    if duplicados:
        detalhe = ", ".join(
            "%s (%d linhas)" % (p, n) for p, n in sorted(duplicados.items())
        )
        raise ValueError(
            "periodo duplicado apos o filtro de 'Total': %s. "
            "O filtro de dimensoes extras deixou passar mais de um recorte "
            "para o mesmo mes; corrija o filtro antes de aceitar o CSV."
            % detalhe
        )

    linhas = list(linhas_por_periodo.values())
    linhas.sort(key=lambda l: l["periodo"])
    return linhas
```

Trocar `main()` inteiro por:

```python
def _escrever(caminho, linhas):
    with open(caminho, "w", encoding="utf-8", newline="") as fh:
        escritor = csv.DictWriter(fh, fieldnames=["periodo", "valor", "unidade"])
        escritor.writeheader()
        escritor.writerows(linhas)
    print("  %d linhas, de %s a %s"
          % (len(linhas), linhas[0]["periodo"], linhas[-1]["periodo"]))


def main():
    os.makedirs(DESTINO, exist_ok=True)
    os.makedirs(DESTINO_MENSAL, exist_ok=True)
    for tabela, variavel, nome in SERIES:
        print("baixando tabela %s (variavel %s) para %s" % (tabela, variavel, nome))
        _escrever(os.path.join(DESTINO, nome),
                  extrair_linhas(baixar(URL % (tabela, variavel))))
        print("baixando tabela %s (variavel %s) para mensal/%s" % (tabela, variavel, nome))
        _escrever(os.path.join(DESTINO_MENSAL, nome),
                  extrair_linhas_mensais(baixar(URL_MENSAL % (tabela, variavel))))
```

E trocar a assinatura de `baixar` para receber a URL pronta:

```python
def baixar(url):
    with urllib.request.urlopen(url, timeout=120) as resposta:
        return json.loads(resposta.read().decode("utf-8"))
```

- [ ] **Step 4: Gerar os CSVs mensais**

Run: `python3 tools/baixar_dados.py`
Expected: dez blocos de saída, cinco em `dados/` e cinco em `dados/mensal/`. As três séries de abate imprimem `351 linhas, de 1997-01 a 2026-03`; ovos imprime `471 linhas, de 1987-01 a 2026-03`.

Conferir que o trimestral não mudou: `git diff --stat dados/*.csv` deve sair vazio.

- [ ] **Step 5: Rodar os testes e ver passar**

Run: `python3 -m pytest tools/tests/test_dados_mensal.py tools/tests/test_dados.py -v`
Expected: PASS em todos.

- [ ] **Step 6: Atualizar `dados/README.md`**

Substituir a seção "As séries são trimestrais, não mensais" por "As duas granularidades", declarando: que `c12716` existe e traz os três meses; que `dados/` continua trimestral porque as Aulas 01 a 06 leem dela; que `dados/mensal/` vale da Aula 07 em diante; que o contrato mensal é `AAAA-MM`; que a reconciliação é exata nas três séries de abate e diverge em 1 unidade em 59 dos 157 trimestres de ovos e 39 dos 117 de leite, por arredondamento independente de cada mês em "Mil dúzias" e "Mil litros".

Reescrever a seção "TAPI pede mensal, o dado aberto é trimestral" como "O horizonte de 24 meses agora é medido em meses", registrando que a contraproposta de 8 trimestres da ART.1 deixa de ser necessária e que a `ADR-010` explica a mudança.

Acrescentar a cada uma das cinco séries a linha `Período coberto (mensal)`.

- [ ] **Step 7: Commit**

```bash
git add tools/baixar_dados.py dados/mensal dados/README.md tools/tests/test_dados_mensal.py
git commit -m "feat(dados): baixa as cinco series do SIDRA em base mensal

As cinco tabelas trazem a classificacao c12716 (Referencia temporal) com
'No 1o mes', 'No 2o mes' e 'No 3o mes' alem de 'Total do trimestre'. O
script pedia a serie sem essa classificacao e o filtro de 'Total'
selecionava o trimestre em silencio.

dados/ segue trimestral, porque as Aulas 01 a 06 leem dela e citam achados
medidos nela. dados/mensal/ vale da Aula 07 em diante.

A soma dos tres meses reconcilia exatamente com o trimestre ja versionado
nas tres series de abate, e diverge em 1 unidade em ovos e leite, por
arredondamento independente de cada mes."
```

---

### Task 2: Os quatro vereditos travados em teste

**Files:**
- Create: `tools/tests/test_modelos_aula07.py`
- Modify: `requirements-ci.txt`

**Interfaces:**
- Consumes: `dados/mensal/<serie>.csv` da Task 1.
- Produces: a fixture `base()` que devolve `dict` de colunas em `numpy.ndarray`, com as chaves `periodo`, as cinco séries, `mes`, `dias`, `sen`, `cos`, `lag1`, `lag2`, `lag3`, `lag12` e `<serie>_lag1`. Os helpers `_ajustar(dados, features, modelo, alvo_em_razao=False, padronizar=False)`, `_mape(real, previsto)` e `_rmse(real, previsto)`.

- [ ] **Step 1: Escrever o teste que falha**

Criar `tools/tests/test_modelos_aula07.py`. O arquivo trava as quatro hipóteses da aula como conclusões, com faixas estreitas o bastante para falhar se a conclusão virar, e largas o bastante para não quebrar por versão de scikit-learn:

```python
"""Trava as quatro hipoteses que a Aula 07 declara e testa em sala.

O deck, o material, as notas do professor e o notebook citam metricas de
modelos ajustados sobre dados/mensal/. Se um CSV for regerado com dado novo do
SIDRA, uma conclusao pode virar, e os quatro artefatos passam a ensinar algo
que o dado nao sustenta.

Como em test_hipoteses_aula04.py e test_modelo_aula05.py, o que se trava aqui
sao as conclusoes:

  H1  a granularidade do case nao e imposta pela fonte     -> test_dados_mensal.py
  H2  a arvore nao ganha da reta nem da baseline           -> falsa, medida aqui
  H3  a arvore perde porque nao extrapola tendencia        -> verdadeira
  H4  alvo em razao devolve a arvore a disputa             -> verdadeira

Mais dois achados que a aula usa e que precisam continuar valendo:

  5  padronizar piora o KNN nesta base, em todo k testado
  6  a regressao linear sobre a razao com 11 features bate a baseline da LDC

Precisa de numpy e scikit-learn, do requirements-ci.txt. Sem eles o arquivo
inteiro se pula, e o CI continua cobrindo o resto.

Cada assercao foi vista falhando ao menos uma vez, contra tres versoes
propositalmente quebradas: treinar a arvore no alvo em razao dentro do bloco de
H2 (derruba test_arvore_perde_da_reta_e_da_baseline), embaralhar as linhas antes
do corte temporal (derruba os testes de teto) e padronizar o KNN dentro do bloco
que afirma que padronizar piora (derruba test_padronizar_piora_o_knn).
"""
import calendar
import csv
import os

import pytest

np = pytest.importorskip("numpy")
pytest.importorskip("sklearn")

from sklearn.ensemble import RandomForestRegressor  # noqa: E402
from sklearn.linear_model import LinearRegression  # noqa: E402
from sklearn.neighbors import KNeighborsRegressor  # noqa: E402
from sklearn.preprocessing import StandardScaler  # noqa: E402
from sklearn.tree import DecisionTreeRegressor  # noqa: E402

RAIZ = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
MENSAL = os.path.join(RAIZ, "dados", "mensal")

SERIES = ["abate_bovinos", "abate_suinos", "abate_frangos",
          "producao_ovos", "producao_leite"]
ALVO = "abate_frangos"
FEATURES = ["lag1", "lag12", "sen", "cos"]
FEATURES_FECHO = (["lag1", "lag2", "lag3", "lag12", "sen", "cos", "dias"]
                  + [s + "_lag1" for s in SERIES if s != ALVO])
N_TESTE = 24
SEMENTE = 42


def _serie(nome):
    caminho = os.path.join(MENSAL, nome + ".csv")
    with open(caminho, encoding="utf-8") as fh:
        return {l["periodo"]: float(l["valor"])
                for l in csv.DictReader(fh) if l["valor"]}


@pytest.fixture(scope="module")
def base():
    series = {s: _serie(s) for s in SERIES}
    periodos = sorted(set.intersection(*[set(v) for v in series.values()]))

    col = {"periodo": periodos}
    for s in SERIES:
        col[s] = [series[s][p] for p in periodos]
    col["mes"] = [int(p.split("-")[1]) for p in periodos]
    col["dias"] = [calendar.monthrange(int(p.split("-")[0]), int(p.split("-")[1]))[1]
                   for p in periodos]
    col["sen"] = [np.sin(2 * np.pi * m / 12) for m in col["mes"]]
    col["cos"] = [np.cos(2 * np.pi * m / 12) for m in col["mes"]]

    def defasar(valores, k):
        return [None] * k + list(valores[:-k])

    for k in (1, 2, 3, 12):
        col["lag%d" % k] = defasar(col[ALVO], k)
    for s in SERIES:
        if s != ALVO:
            col[s + "_lag1"] = defasar(col[s], 1)

    manter = [i for i in range(len(periodos))
              if all(col[c][i] is not None for c in col)]
    saida = {c: ([col[c][i] for i in manter] if c == "periodo"
                 else np.array([col[c][i] for i in manter], dtype=float))
             for c in col}
    return saida


def _fatiar(dados, features, alvo_em_razao):
    n = len(dados["periodo"])
    corte = n - N_TESTE
    X = np.column_stack([dados[f] for f in features])
    y = dados[ALVO]
    alvo = y / dados["lag12"] if alvo_em_razao else y
    return (X[:corte], alvo[:corte], X[corte:], y[corte:], dados["lag12"][corte:])


def _ajustar(dados, features, modelo, alvo_em_razao=False, padronizar=False):
    Xtr, alvo_tr, Xte, yte, lag12te = _fatiar(dados, features, alvo_em_razao)
    if padronizar:
        escalador = StandardScaler().fit(Xtr)
        Xtr, Xte = escalador.transform(Xtr), escalador.transform(Xte)
    previsto = modelo.fit(Xtr, alvo_tr).predict(Xte)
    if alvo_em_razao:
        previsto = previsto * lag12te
    return yte, previsto


def _mape(real, previsto):
    return float(np.mean(np.abs((real - previsto) / real)) * 100)


def _rmse(real, previsto):
    return float(np.sqrt(np.mean((real - previsto) ** 2)))


def _baselines(dados):
    n = len(dados["periodo"])
    corte = n - N_TESTE
    fator = float(np.mean(dados[ALVO][:corte] / dados["lag12"][:corte]))
    return {
        "A": dados["lag1"][corte:],
        "B": dados["lag12"][corte:],
        "C": dados["lag12"][corte:] * fator,
    }, fator


# --------------------------------------------------------------------------
# A base


def test_base_tem_o_tamanho_que_a_aula_afirma(base):
    assert len(base["periodo"]) == 339
    assert base["periodo"][0] == "1998-01"
    assert base["periodo"][-1] == "2026-03"
    corte = len(base["periodo"]) - N_TESTE
    assert corte == 315
    assert base["periodo"][corte - 1] == "2024-03"
    assert base["periodo"][corte] == "2024-04"


def test_o_teste_cobre_os_24_meses_do_horizonte_do_tapi(base):
    assert N_TESTE == 24


# --------------------------------------------------------------------------
# Baselines


def test_a_baseline_da_ldc_e_a_melhor_das_tres(base):
    linhas, fator = _baselines(base)
    yte = base[ALVO][-N_TESTE:]
    mapes = {k: _mape(yte, v) for k, v in linhas.items()}
    assert mapes["C"] < mapes["B"] < mapes["A"]
    assert 6.5 < mapes["A"] < 7.0
    assert 4.9 < mapes["B"] < 5.3
    assert 3.5 < mapes["C"] < 3.9
    assert 1.05 < fator < 1.06


# --------------------------------------------------------------------------
# H2: a arvore nao ganha da reta nem da baseline


def test_arvore_perde_da_reta_e_da_baseline(base):
    """H2 e falsa. Este e o teste que a aula inteira depende de continuar
    falhando a hipotese: se a arvore passar a ganhar, o roteiro nao vale."""
    linhas, _ = _baselines(base)
    yte = base[ALVO][-N_TESTE:]
    _, p_arvore = _ajustar(
        base, FEATURES, DecisionTreeRegressor(max_depth=3, random_state=SEMENTE))
    _, p_reta = _ajustar(
        base, FEATURES, LinearRegression(), padronizar=True)

    mape_arvore = _mape(yte, p_arvore)
    mape_reta = _mape(yte, p_reta)
    mape_baseline = _mape(yte, linhas["C"])

    assert mape_arvore > mape_reta
    assert mape_arvore > mape_baseline
    assert 7.2 < mape_arvore < 8.1
    assert 4.0 < mape_reta < 4.5
    assert 100e6 < _rmse(yte, p_arvore) < 120e6


def test_a_floresta_melhora_a_arvore_e_ainda_perde_da_baseline(base):
    linhas, _ = _baselines(base)
    yte = base[ALVO][-N_TESTE:]
    _, p_arvore = _ajustar(
        base, FEATURES, DecisionTreeRegressor(max_depth=3, random_state=SEMENTE))
    _, p_floresta = _ajustar(
        base, FEATURES,
        RandomForestRegressor(n_estimators=300, random_state=SEMENTE))
    assert _mape(yte, p_floresta) < _mape(yte, p_arvore)
    assert _mape(yte, p_floresta) > _mape(yte, linhas["C"])
    assert 5.0 < _mape(yte, p_floresta) < 5.7


# --------------------------------------------------------------------------
# H3: o teto da arvore


def test_a_arvore_tem_teto_e_o_teste_esta_acima_dele(base):
    """H3 e verdadeira. A arvore preve a media da folha, entao a maior
    previsao possivel e a media da folha mais alta. O alvo cresce, e o teste
    fica quase todo acima desse teto."""
    Xtr, ytr, Xte, yte, _ = _fatiar(base, FEATURES, alvo_em_razao=False)
    arvore = DecisionTreeRegressor(max_depth=3, random_state=SEMENTE).fit(Xtr, ytr)
    previsto = arvore.predict(Xte)

    teto = float(previsto.max())
    assert teto < float(ytr.max()), "o teto e a media de uma folha, nao o maximo do treino"
    assert int((yte > teto).sum()) == 23
    assert int((yte > ytr.max()).sum()) == 5
    assert arvore.get_n_leaves() == 8
    assert 1.05e9 < teto < 1.13e9
    assert 1.29e9 < float(yte.max()) < 1.32e9


def test_a_reta_nao_tem_teto(base):
    """O contraste que explica H3: a reta extrapola, a arvore nao."""
    Xtr, ytr, _, _, _ = _fatiar(base, FEATURES, alvo_em_razao=False)
    _, p_reta = _ajustar(base, FEATURES, LinearRegression(), padronizar=True)
    assert float(p_reta.max()) > float(ytr.max())


# --------------------------------------------------------------------------
# H4: alvo em razao


def test_alvo_em_razao_corta_o_erro_da_arvore_pela_metade(base):
    """H4 e verdadeira."""
    yte = base[ALVO][-N_TESTE:]
    _, p_nivel = _ajustar(
        base, FEATURES, DecisionTreeRegressor(max_depth=3, random_state=SEMENTE))
    _, p_razao = _ajustar(
        base, FEATURES, DecisionTreeRegressor(max_depth=3, random_state=SEMENTE),
        alvo_em_razao=True)
    assert _mape(yte, p_razao) < _mape(yte, p_nivel) / 1.8
    assert 3.6 < _mape(yte, p_razao) < 4.1


def test_alvo_em_razao_melhora_todos_os_modelos_que_nao_extrapolam(base):
    yte = base[ALVO][-N_TESTE:]
    for rotulo, modelo, padronizar in [
        ("arvore", DecisionTreeRegressor(max_depth=3, random_state=SEMENTE), False),
        ("floresta", RandomForestRegressor(n_estimators=300, random_state=SEMENTE), False),
        ("knn padronizado", KNeighborsRegressor(n_neighbors=5), True),
        ("knn sem padronizar", KNeighborsRegressor(n_neighbors=5), False),
    ]:
        _, p_nivel = _ajustar(base, FEATURES, modelo, padronizar=padronizar)
        _, p_razao = _ajustar(base, FEATURES, modelo, alvo_em_razao=True,
                              padronizar=padronizar)
        assert _mape(yte, p_razao) < _mape(yte, p_nivel), rotulo


def test_a_reta_piora_com_alvo_em_razao(base):
    """A reta ja extrapolava, entao a razao tira dela informacao de nivel.
    O contraste impede a leitura de que razao e sempre melhor."""
    yte = base[ALVO][-N_TESTE:]
    _, p_nivel = _ajustar(base, FEATURES, LinearRegression(), padronizar=True)
    _, p_razao = _ajustar(base, FEATURES, LinearRegression(),
                          alvo_em_razao=True, padronizar=True)
    assert _mape(yte, p_razao) > _mape(yte, p_nivel)


# --------------------------------------------------------------------------
# Achado 5: padronizar piora o KNN nesta base


def test_padronizar_piora_o_knn(base):
    yte = base[ALVO][-N_TESTE:]
    for k in (3, 5, 10):
        _, com = _ajustar(base, FEATURES, KNeighborsRegressor(n_neighbors=k),
                          padronizar=True)
        _, sem = _ajustar(base, FEATURES, KNeighborsRegressor(n_neighbors=k),
                          padronizar=False)
        assert _mape(yte, sem) < _mape(yte, com), k


def test_sem_padronizar_a_distancia_e_so_das_defasagens(base):
    """O motivo medido do achado 5: sen e cos nao participam da distancia."""
    Xtr, _, _, _, _ = _fatiar(base, FEATURES, alvo_em_razao=False)
    variancias = Xtr.var(axis=0)
    participacao = variancias / variancias.sum()
    porcento = dict(zip(FEATURES, participacao * 100))
    assert porcento["lag1"] + porcento["lag12"] > 99.99
    assert porcento["sen"] < 1e-6
    assert porcento["cos"] < 1e-6


# --------------------------------------------------------------------------
# Achado 6: o modelo do fecho bate a baseline da LDC


def test_o_modelo_do_fecho_bate_a_baseline_da_ldc(base):
    linhas, _ = _baselines(base)
    yte = base[ALVO][-N_TESTE:]
    _, previsto = _ajustar(base, FEATURES_FECHO, LinearRegression(),
                           alvo_em_razao=True, padronizar=True)
    assert len(FEATURES_FECHO) == 11
    assert _mape(yte, previsto) < _mape(yte, linhas["C"])
    assert _rmse(yte, previsto) < _rmse(yte, linhas["C"])
    assert 3.1 < _mape(yte, previsto) < 3.5
    assert 44e6 < _rmse(yte, previsto) < 50e6


def test_o_fecho_ganha_de_todos_os_modelos_da_aula(base):
    yte = base[ALVO][-N_TESTE:]
    _, fecho = _ajustar(base, FEATURES_FECHO, LinearRegression(),
                        alvo_em_razao=True, padronizar=True)
    for modelo, razao, padronizar in [
        (DecisionTreeRegressor(max_depth=3, random_state=SEMENTE), True, False),
        (RandomForestRegressor(n_estimators=300, random_state=SEMENTE), True, False),
        (KNeighborsRegressor(n_neighbors=5), True, False),
        (LinearRegression(), False, True),
    ]:
        _, p = _ajustar(base, FEATURES, modelo, alvo_em_razao=razao,
                        padronizar=padronizar)
        assert _mape(yte, fecho) < _mape(yte, p)
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `python3 -m pytest tools/tests/test_modelos_aula07.py -v`
Expected: FAIL, porque `dados/mensal/` só existe se a Task 1 rodou. Se a Task 1 já rodou, os testes devem passar direto; nesse caso, provar que travam a conclusão rodando as três mutações do docstring e vendo cada uma derrubar ao menos um teste.

- [ ] **Step 3: Provar que os testes travam conclusão, não só número**

Rodar as três mutações, uma por vez, e registrar qual teste cai:

1. Em `test_arvore_perde_da_reta_e_da_baseline`, trocar `_ajustar(base, FEATURES, DecisionTreeRegressor(...))` por `_ajustar(base, FEATURES, DecisionTreeRegressor(...), alvo_em_razao=True)`. Expected: FAIL em `assert mape_arvore > mape_reta`.
2. Em `_fatiar`, embaralhar as linhas antes do corte. Expected: FAIL em `test_a_arvore_tem_teto_e_o_teste_esta_acima_dele`.
3. Em `test_padronizar_piora_o_knn`, trocar `padronizar=False` por `padronizar=True` nas duas chamadas. Expected: FAIL em `assert _mape(yte, sem) < _mape(yte, com)`.

Desfazer as três mutações depois.

- [ ] **Step 4: Rodar a suíte inteira**

Run: `python3 -m pytest tools/tests/ -v`
Expected: PASS em tudo, incluindo os testes das Aulas 04, 05 e 06, que não podem ter sido afetados.

- [ ] **Step 5: Commit**

```bash
git add tools/tests/test_modelos_aula07.py
git commit -m "test(aula07): trava os quatro vereditos das hipoteses da aula

H2 (a arvore ganha da reta) e falsa, H3 (a arvore perde porque nao
extrapola) e verdadeira, H4 (alvo em razao devolve a arvore a disputa) e
verdadeira. Mais dois achados: padronizar piora o KNN nesta base em todo k
testado, e a regressao sobre a razao com 11 features bate a baseline da LDC.

As tres mutacoes do docstring foram rodadas e cada uma derruba ao menos um
teste."
```

---

### Task 3: As duas figuras

**Files:**
- Create: `tools/graficos_aula07.py`
- Create: `assets/img/aula07-teto-da-arvore.png`, `assets/img/aula07-nivel-versus-razao.png`

**Interfaces:**
- Consumes: `dados/mensal/` da Task 1; a mesma definição de base da Task 2.
- Produces: dois PNG em `assets/img/`, 1600x900 a 150 dpi, cores lidas de `assets/css/inteli-brand.css`.

- [ ] **Step 1: Escrever o script**

`tools/graficos_aula07.py` gera duas figuras. Cores: ler os valores hexadecimais dos tokens de `assets/css/inteli-brand.css` no topo do script, com um comentário dizendo de qual token cada um veio, como `tools/graficos_aula06.py` já faz.

**Figura 1, `aula07-teto-da-arvore.png`.** Um painel único, eixo x de 2024-04 a 2026-03 (os 24 meses de teste), eixo y em bilhões de quilogramas:
- linha cheia: `abate_frangos` real;
- linha escalonada (`drawstyle="steps-mid"`): a previsão da árvore, que visivelmente se repete em patamares;
- linha horizontal tracejada no teto 1.090.166.234, rotulada "teto da árvore: média da folha mais alta";
- linha horizontal pontilhada no máximo do treino 1.226.709.256, rotulada "máximo do treino, 2023-03";
- sombreado leve na região acima do teto, com a anotação "23 dos 24 meses de teste estão aqui".

**Figura 2, `aula07-nivel-versus-razao.png`.** Dois painéis lado a lado, mesmo eixo x e mesmo eixo y:
- esquerda, alvo em nível: real contra previsão da árvore, título do painel "alvo em nível, MAPE 7,66%";
- direita, alvo em razão: real contra previsão da árvore, título do painel "alvo em razão, MAPE 3,86%".

Os dois títulos de painel são rótulos de referência, não action titles: a conclusão fica no título do slide que usa a figura.

- [ ] **Step 2: Gerar as figuras**

Run: `python3 tools/graficos_aula07.py`
Expected: dois arquivos escritos em `assets/img/`, com as dimensões impressas na saída.

- [ ] **Step 3: Conferir à vista**

Abrir os dois PNG. Critérios: nenhum texto cortado; a escada da previsão da árvore visível na figura 1; os dois painéis da figura 2 com o mesmo limite de eixo y, senão a comparação engana.

- [ ] **Step 4: Rodar o validador de marca**

Run: `python3 tools/check_brand.py`
Expected: PASS. Se reprovar por cor literal, mover a cor para leitura do token.

- [ ] **Step 5: Commit**

```bash
git add tools/graficos_aula07.py assets/img/aula07-teto-da-arvore.png assets/img/aula07-nivel-versus-razao.png
git commit -m "feat(aula07): figuras do teto da arvore e do alvo em razao"
```

---

### Task 4: Notebook da aula

**Files:**
- Create: `notebooks/aula07.ipynb`

**Interfaces:**
- Consumes: `dados/mensal/` da Task 1; os números canônicos da tabela de Global Constraints.
- Produces: a base analítica de 339 linhas na variável `base`, e o quadro de hipóteses preenchido na última célula.

- [ ] **Step 1: Escrever o notebook**

Seis seções, cada uma fechando uma hipótese. A primeira célula traz o badge do Colab e o `try`/`except` em português que a Aula 06 instalou, orientando a dupla a pedir a pasta `dados` para uma dupla vizinha se a rede cair.

| seção | conteúdo | critério de sucesso impresso |
|---|---|---|
| 1. A base mensal | monta a base a partir de `dados/mensal/`, com a definição única das Global Constraints | `base.shape` sai `(339, ...)`, primeiro período `1998-01`, último `2026-03` |
| 2. H1, a reconciliação | soma os três meses de cada trimestre e compara com `dados/` | zero divergência nas três séries de abate |
| 3. H2, a árvore perde | treina árvore, reta e as três baselines, imprime a tabela | árvore 7,66%, reta 4,25%, baseline C 3,71% |
| 4. H3, o teto | `arvore.predict(X_teste).max()` e a contagem de meses acima | teto 1.090.166.234, 23 de 24 acima |
| 5. KNN e a distância | KNN com e sem padronizar, e a tabela de participação na distância | k=5: 9,37% padronizado, 5,62% sem |
| 6. H4, o alvo em razão | árvore, floresta e KNN sobre a razão, mais o modelo do fecho | árvore 3,86%, fecho 3,32% |

A célula final imprime o quadro das quatro hipóteses com veredito e evidência, que é o que a dupla copia para a ART.6.

Random Forest aparece só na seção 6, em duas linhas, sem teoria de bagging.

- [ ] **Step 2: Executar o notebook do início ao fim**

Run: `jupyter nbconvert --to notebook --execute --inplace notebooks/aula07.ipynb`
Expected: execução sem erro. Conferir na saída que cada critério de sucesso da tabela acima aparece com o valor esperado.

- [ ] **Step 3: Conferir que os números batem com o teste**

Run: `python3 -m pytest tools/tests/test_modelos_aula07.py -v`
Expected: PASS. Se um número do notebook divergir do teste, o notebook está errado: o teste é a fonte.

- [ ] **Step 4: Commit**

```bash
git add notebooks/aula07.ipynb
git commit -m "feat(aula07): notebook com as quatro hipoteses executadas"
```

---

### Task 5: Deck

**Files:**
- Create: `aulas/aula07.html`

**Interfaces:**
- Consumes: as figuras da Task 3, os números da Task 2.
- Produces: nada que outra task consuma.

- [ ] **Step 1: Ler a anatomia do deck antes de escrever**

Abrir `~/.claude/skills/inteli-deck-design/SKILL.md` seção 7 (ordem canônica dos blocos, classes, markup de quiz) e `aulas/_fixture-tema.html` (deck de referência de cada classe). Não reinventar markup.

Abrir `aulas/aula06.html` para copiar o padrão do cabeçalho em comentário, que registra roteiro, escopo e a origem de cada número.

- [ ] **Step 2: Escrever o deck**

Cerca de 30 slides, nesta ordem:

| slides | bloco |
|---|---|
| 1 | capa (`conic-gradient`, nunca SVG recortado) |
| 2-3 | resgate da Aula 06 e agenda |
| 4-6 | H1: o que o SIDRA tem, a classificação `c12716`, a reconciliação |
| 7-8 | a base mensal de 339 linhas, treino e teste |
| 9-11 | árvore de decisão: o que ela faz, sem entropia à mão |
| 12-14 | H2: a tabela de resultados, com a árvore em último |
| 15-17 | H3: o teto, a figura 1, a contagem de 23 em 24 |
| 18-21 | KNN: semelhança, a tabela de participação na distância, o achado de padronização |
| 22-24 | H4: a figura 2, a tabela de nível contra razão |
| 25 | Random Forest, um slide |
| 26 | o modelo do fecho, 3,32% contra 3,71% |
| 27-28 | quiz |
| 29-30 | ART.6, o quadro de hipóteses e o autoestudo da próxima aula |

Regras de título, aplicadas slide a slide:
- Slide analítico leva a conclusão com o número dentro. Exemplo aceitável: "A árvore erra 7,66% e fica atrás das três baselines". Exemplo proibido: "Árvore de decisão: o que ela captura".
- Slide de referência leva rótulo curto. Exemplo: "Agenda do encontro", "Checklist da ART.6".
- Proibido paralelismo negativo, antítese simétrica, escalada com dois-pontos e título de listicle contado.

O slide do teto (15 a 17) é o que não pode ser cortado ao vivo, e isso vai nas notas do professor.

- [ ] **Step 3: Rodar os validadores**

Run: `python3 tools/check_slides.py aulas/aula07.html`
Expected: PASS, sem estouro nem sobreposição.

Run: `python3 tools/check_brand.py`
Expected: PASS.

Run: `python3 tools/check_links.py`
Expected: PASS.

- [ ] **Step 4: Conferir a folga vertical dos slides densos**

Run: `python3 tools/check_slides.py aulas/aula07.html --shots /tmp/shots-aula07`
Expected: PASS. Abrir os screenshots dos slides de tabela (12 a 14, 18 a 21, 22 a 24). Qualquer slide com menos de 80px de folga vertical no macOS é candidato a reprovar no Ubuntu do CI: encolher a tabela ou quebrar em dois slides antes de commitar.

- [ ] **Step 5: Testar o estado pós-interação à mão**

`check_slides.py` mede o deck com `page.goto` e nunca clica em nada. Servir o acervo e conferir na mão:

Run: `python3 -m http.server 8000`
Abrir `http://localhost:8000/aulas/aula07.html`, responder o quiz dos slides 27 e 28 e revelar todos os fragments. Critério: a resposta certa e as erradas mudam de aparência de verdade, não só no DOM.

- [ ] **Step 6: Commit**

```bash
git add aulas/aula07.html
git commit -m "feat(aula07): deck com as quatro hipoteses, arvore, KNN e floresta"
```

---

### Task 6: Material de apoio

**Files:**
- Create: `materiais/aula07.html`

**Interfaces:**
- Consumes: os números da Task 2, o estilo de `assets/css/inteli-material.css`.

- [ ] **Step 1: Escrever o material**

Nove seções, com a TOC lateral que `assets/css/inteli-material.css` já fornece:

1. O que a Aula 06 deixou pronto.
2. H1: a classificação `c12716` e por que a leitura anterior da fonte estava incompleta.
3. A base mensal: contrato, 339 linhas, corte temporal de 24 meses.
4. Árvore de decisão: partir o espaço, prever a média da folha.
5. H2: a tabela completa de RMSE e MAPE.
6. H3: o teto, com os três números (máximo do treino, teto, máximo do teste) e a conta dos 23 em 24.
7. KNN: semelhança, e a seção que declara que a regra da Aula 05 não é revogada, com a tabela de participação na distância.
8. H4: alvo em razão, e o contraste de que a reta piora com razão.
9. Random Forest em três parágrafos, e o modelo do fecho.

A seção 7 precisa dizer explicitamente que padronizar iguala a influência das features na distância, o que é o certo quando não se sabe qual importa e o errado quando se sabe, e apontar para a seção 9 do material da Aula 05.

- [ ] **Step 2: Rodar os validadores**

Run: `python3 tools/check_brand.py && python3 tools/check_links.py`
Expected: PASS.

- [ ] **Step 3: Conferir o layout abaixo de 900px**

Servir o acervo e abrir `http://localhost:8000/materiais/aula07.html` numa janela de 800px de largura. Critério: a TOC sai do layout e o grid volta para uma coluna sem deixar buraco.

- [ ] **Step 4: Commit**

```bash
git add materiais/aula07.html
git commit -m "docs(aula07): material de apoio"
```

---

### Task 7: Referências

**Files:**
- Create: `referencias/aula07.html`

**Interfaces:**
- Consumes: `docs/autoestudos-por-semana.md`, Semana 05.

- [ ] **Step 1: Conferir a lista contra a fonte**

Run: `awk '/## Semana 05/,/## Semana 06/' docs/autoestudos-por-semana.md`

A Semana 05 tem dezessete autoestudos. Cinco já estão em `referencias/aula06.html` (Determinando K: Elbow Plot · Determinando K: Silhouette Analysis · Introdução ao aprendizado não supervisionado (IBM) · K-means · Opcional: PCA). Os doze restantes vão para esta página, **com o título exato da fonte**:

1. Matriz de confusão, precisão e revocação
2. Naive Bayes
3. Opcional: Feature Engineering - Notebook com respostas
4. Opcional: Prática - Regressão Logística
5. Opcional: Prática - SVM - Support Vector Machine
6. Opcional: Random Forest - Exemplo prático
7. Opcional: Árvore de Decisão com cálculo de Entropia passo a passo
8. Random Forest
9. Regressão Logística
10. SVM - Support Vector Machine
11. Árvore de Decisão e Entropia
12. Árvore de Decisão na prática

- [ ] **Step 2: Escrever a página**

Seguir o padrão de `referencias/aula06.html`: referências numeradas, seção de autoestudos da semana e seção de leitura complementar.

Marcar, na própria página, quais dos doze a aula de hoje cobre e quais a Aula 09 assume, para o aluno que leu tudo não achar que a lista está errada. Os cobertos hoje são "Random Forest", "Árvore de Decisão e Entropia" e "Árvore de Decisão na prática", parcialmente. Os demais migram.

- [ ] **Step 3: Rodar os validadores**

Run: `python3 tools/check_brand.py && python3 tools/check_links.py`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add referencias/aula07.html
git commit -m "docs(aula07): referencias e os doze autoestudos restantes da Semana 05"
```

---

### Task 8: Notas do professor

**Files:**
- Create: `docs/notas-do-professor/aula07.md`

- [ ] **Step 1: Escrever as notas**

Seguir a estrutura de `docs/notas-do-professor/aula06.md`:

- **Onde está o peso da aula:** os slides 15 a 17, o teto da árvore. Um aluno que saia sabendo rodar `DecisionTreeRegressor` e sem saber que ela não extrapola não aprendeu o que a aula ensina.
- **Checkpoint de abertura:** quais duplas conseguem montar a base mensal. Pedir o `shape`, que é `(339, ...)`. Quem não tiver, abrir o notebook pelo badge do Colab.
- **Ordem de corte se o tempo apertar:** Random Forest sai primeiro, depois o modelo do fecho, depois a tabela de participação na distância do KNN. O bloco do teto e o bloco de H4 não saem.
- **A resposta de trinta segundos sobre o autoestudo:** a turma leu matriz de confusão, Naive Bayes, regressão logística e SVM para hoje e a aula não toca neles. A resposta é que o case é de regressão, que classificar mês em "alta" e "baixa" exigiria inventar um rótulo que o dado não tem, e que a Aula 09 assume os quatro. Registrado na `ADR-010`.
- **A pergunta que abre cada bloco**, com a resposta esperada e o erro que ela revela, para os oito blocos do roteiro.
- **O risco de leitura errada:** duas leituras a desfazer em voz alta, que árvore é modelo ruim e que padronizar é errado.

- [ ] **Step 2: Commit**

```bash
git add docs/notas-do-professor/aula07.md
git commit -m "docs(aula07): notas do professor"
```

---

### Task 9: ADR-010 e a revisão da ADR-003

**Files:**
- Create: `docs/adrs/ADR-010-base-mensal-e-escopo-da-aula-07.md`
- Modify: `docs/adrs/ADR-003-regressao-tabular-em-vez-de-series-temporais.md`

- [ ] **Step 1: Escrever a ADR-010**

Estrutura mínima das diretivas globais: Data, Status, Decisores, Contexto, Decisão, Motivações, Riscos conhecidos com mitigação, Consequências positivas e negativas, ADRs relacionadas.

Duas decisões num único ADR, porque são a mesma conversa:

1. **A base do case passa a ser mensal, da Aula 07 em diante.** `dados/` trimestral fica, porque quatro aulas publicadas leem dela. Revoga da `ADR-003` a afirmação "Não existe versão mensal aberta dessas séries". Mantém de pé a decisão principal da `ADR-003`, regressão tabular em vez de séries temporais, e o corte temporal por data. O horizonte de 8 trimestres vira 24 meses, que são o mesmo intervalo.
2. **O escopo da Aula 07 cai de sete assuntos para três.** Ficam árvore de decisão, KNN e Random Forest. Migram para a Aula 09: matriz de confusão, precisão, revocação, Naive Bayes, regressão logística, SVM e entropia calculada à mão.

Registrar em Riscos que a tabela de riscos da `ADR-003` previa "a turma achar que trimestral foi preguiça nossa, não limitação da fonte" e mitigava mandando a turma abrir a interface do IBGE; o risco se materializou por um motivo que a mitigação não cobria, que é a leitura incompleta da fonte.

Registrar em Consequências negativas a dívida que a Aula 09 herda, com o mesmo cuidado com que a `ADR-009` registrou a dívida da Aula 08.

- [ ] **Step 2: Atualizar a ADR-003**

Trocar o `**Status:** Aceita` por `**Status:** Aceita, parcialmente revista pela ADR-010`.

Acrescentar, logo abaixo do cabeçalho, uma nota de revisão de três linhas apontando qual afirmação caiu e qual continua de pé. Não reescrever o corpo: um ADR registra o que se decidiu quando se decidiu.

- [ ] **Step 3: Commit**

```bash
git add docs/adrs/
git commit -m "docs(adr): ADR-010, base mensal e reducao de escopo da Aula 07

Revoga da ADR-003 a afirmacao de que nao existe versao mensal aberta das
cinco series. A classificacao c12716 do SIDRA entrega os tres meses de cada
trimestre nas cinco tabelas. A decisao principal da ADR-003, regressao
tabular em vez de series temporais, continua de pe."
```

---

### Task 10: Amarração do acervo

**Files:**
- Modify: `index.html`
- Modify: `PLANEJAMENTO_AULA_A_AULA.md`
- Modify: `PLANO_DE_ENSINO.md`
- Modify: `docs/ANDAMENTO.md`
- Modify: `requirements-ci.txt`

- [ ] **Step 1: Habilitar o card da Aula 07 no portal**

Em `index.html`, no card da Aula 07 (perto da linha 226), trocar `aria-disabled="true"` pelos quatro `href` reais (`aulas/aula07.html`, `materiais/aula07.html`, `referencias/aula07.html`, `notebooks/aula07.ipynb`) e atualizar o resumo do card para o escopo novo, sem citar matriz de confusão.

- [ ] **Step 2: Atualizar o roteiro**

Em `PLANEJAMENTO_AULA_A_AULA.md`, substituir o roteiro da Aula 07 pelo da seção 5 da spec, e acrescentar à Aula 09 os assuntos que migram. Em `PLANO_DE_ENSINO.md`, atualizar a linha 142 (o que a Aula 06 deixou pronto passa a incluir a base mensal) e qualquer menção à granularidade trimestral como imposição da fonte.

- [ ] **Step 3: Atualizar o ANDAMENTO**

Acrescentar a Aula 07 à seção "Pronto", com os achados medidos. Atualizar a seção "Em andamento" para começar na Aula 08. Acrescentar uma seção "Dívida herdada pela Aula 09, por causa da ADR-010", nos moldes da que existe para a Aula 08.

Corrigir a afirmação da seção de achados da Aula 06 que diz que `referencias/aula07.html` precisa listar os doze autoestudos: agora lista.

- [ ] **Step 4: Atualizar as dependências do CI**

Em `requirements-ci.txt`, a linha do `scikit-learn` cita "Aula 04, StandardScaler e LinearRegression". Estender para citar também a Aula 07 (`DecisionTreeRegressor`, `RandomForestRegressor`, `KNeighborsRegressor`). Nenhum pacote novo é necessário.

- [ ] **Step 5: Rodar a validação inteira**

Run: `python3 tools/check_brand.py && python3 tools/check_slides.py && python3 tools/check_links.py && python3 -m pytest tools/tests/ -v`
Expected: PASS em tudo.

Run: `git status --porcelain`
Expected: nenhum arquivo versionado alterado pela validação. O CI tem um passo que reprova se a validação mexer em arquivo versionado.

- [ ] **Step 6: Commit**

```bash
git add index.html PLANEJAMENTO_AULA_A_AULA.md PLANO_DE_ENSINO.md docs/ANDAMENTO.md requirements-ci.txt
git commit -m "docs(aula07): habilita o card no portal e amarra o roteiro"
```

---

### Task 11: Revisão final

**Files:** nenhum arquivo novo; correções onde a revisão apontar.

- [ ] **Step 1: Rodar o agente `revisor-slides`**

Dispatch do agente `revisor-slides` sobre `aulas/aula07.html`. Ele cobre o que os validadores não pegam: especificidade CSS de estado, estado pós-interação, impressão de verdade e fonte pequena demais para projeção.

- [ ] **Step 2: Conferir a impressão de verdade**

Screenshot de `?print-pdf` não valida impressão, porque só aciona o layout empilhado do Reveal via JavaScript. Gerar o PDF de fato:

Servir o acervo, abrir `http://localhost:8000/aulas/aula07.html?print-pdf` e usar o botão de impressão injetado por `assets/js/inteli-print.js`. Conferir o PDF página a página.

- [ ] **Step 3: Conferir o texto contra as diretivas editoriais**

Run: `grep -rn $'\u2014' aulas/aula07.html materiais/aula07.html referencias/aula07.html docs/notas-do-professor/aula07.md docs/adrs/ADR-010-base-mensal-e-escopo-da-aula-07.md`
Expected: nenhuma ocorrência. O travessão em dash (U+2014) é proibido, e a busca usa o
codepoint para o próprio plano não conter o caractere.

Ler os títulos de todos os slides e conferir contra as três proibições: paralelismo negativo, antítese simétrica e título de listicle contado.

- [ ] **Step 4: Conferir a coerência dos números entre os quatro artefatos**

Escolher cinco números da tabela de Global Constraints (7,66%, 3,86%, 1.090.166.234, 23 de 24, 3,32%) e conferir que aparecem idênticos em deck, material, notebook e notas. Divergência é erro do artefato, não do teste.

- [ ] **Step 5: Rodar a validação completa uma última vez**

Run: `python3 tools/check_brand.py && python3 tools/check_slides.py && python3 tools/check_links.py && python3 -m pytest tools/tests/ -v`
Expected: PASS.

- [ ] **Step 6: Commit e push**

```bash
git add -A
git commit -m "fix(aula07): correcoes da revisao final"
git push
```

Conferir o CI no Ubuntu depois do push: `check_slides.py` passar no macOS não garante que passe lá.
