# Aula 06 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir os quatro artefatos da Aula 06 (01/09/2026), que troca conteúdo novo por retomada das Aulas 01 a 05 e ensina K-means pelo contraste entre dois agrupamentos do mesmo dado.

**Architecture:** Os números vêm primeiro. `tools/tests/test_clusters_aula06.py` trava as conclusões medidas, `tools/graficos_aula06.py` gera as figuras a partir dos mesmos CSVs, e só então o deck, o material, as referências e o notebook citam esses valores. Nenhum artefato calcula um número por conta própria.

**Tech Stack:** HTML e CSS puros com Reveal.js 5.1.0, Python 3 com pandas, numpy, scikit-learn e matplotlib, pytest, Jupyter.

**Spec:** `docs/superpowers/specs/2026-08-31-aula06-consolidacao-e-kmeans-design.md`

## Global Constraints

- Português do Brasil com acentuação completa.
- Travessão em dash (U+2014) é proibido. Usar dois-pontos, vírgula, parênteses ou hífen.
- Emoji é proibido. A iconografia da marca é o Google Material Symbols.
- Nenhuma cor literal nem `font-family` literal fora de `assets/css/inteli-brand.css`. `tools/check_brand.py` reprova.
- Título de slide é frase completa que declara um achado, ou, em slide de referência (agenda, checklist, entregáveis), sintagma curto e descritivo. Título de listicle ("As quatro perguntas do diagnóstico") é proibido.
- Paralelismo negativo ("é X, não Y"), antítese simétrica e escalada com dois-pontos são proibidos.
- Grade de cartões é último recurso. Preferir tabela, depois lista numerada, depois prosa curta.
- O deck é medido em 1280x720 e não rola. Folga vertical mínima de 80px por slide, senão passa no macOS e reprova no Ubuntu do CI.
- Pilha vertical do Reveal (`<section>` aninhada) é proibida: `tools/check_slides.py` só mede `.reveal .slides > section`.
- `footer-page` de um slide é a posição dele entre os slides que têm rodapé, mais 1. `cover-slide`, `section-slide` e `end-slide` não têm rodapé e não incrementam.
- Peso de ART citado de `PLANO_DE_ENSINO.md`. Autoestudo citado de `docs/autoestudos-por-semana.md` com título exato.
- Commits em Conventional Commits, com `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

## Números medidos, fonte única para todos os artefatos

Ato 1, K-means com K=4 sobre as cinco séries padronizadas em nível, 117 linhas de 1997-T1 a 2026-T1:

| Cluster | n | Intervalo |
|---|---|---|
| 2 | 30 | 1997-T1 a 2004-T2 |
| 0 | 28 | 2004-T3 a 2011-T2 |
| 3 | 34 | 2011-T3 a 2020-T2 |
| 1 | 25 | 2019-T3 a 2026-T1 |

Silhueta 0,4795. Inércia 60,21. Concordância com o trimestre do calendário: 26,5%.

Ato 2, K-means com K=4 sobre a participação de cada trimestre no total do próprio ano, 116 linhas de anos completos (1997 a 2025): concordância de 98,3% (114 de 116), silhueta 0,2853, inércia 207,03. As duas exceções são 2008-T2 e 2008-T4, ambas no cluster do T3.

Perfil sazonal, participação no total do ano:

| Trimestre | Bovinos | Suínos | Frangos | Ovos | Leite |
|---|---|---|---|---|---|
| T1 | 23,57% | 23,65% | 24,37% | 24,35% | 25,05% |
| T2 | 24,81% | 24,96% | 24,74% | 24,86% | 23,11% |
| T3 | 25,88% | 26,01% | 25,47% | 25,43% | 24,89% |
| T4 | 25,74% | 25,38% | 25,41% | 25,36% | 26,95% |

Amplitude sazonal em pontos percentuais: leite 3,85, suínos 2,35, bovinos 2,31, frangos 1,10, ovos 1,08.

Números herdados da Aula 05, já travados em `tools/tests/test_modelo_aula05.py`: base analítica de 113 linhas de 1998-T1 a 2026-T1, 105 de treino e 8 de teste; MAPE do modelo 1,60% contra 1,69% da baseline de coeficiente fixo; média de doze janelas 2,18% contra 3,14%; MAPE recursivo de oito passos 2,85%; número de condição 1,08e10 sem padronizar.

---

## Task 1: Testes que travam as conclusões da aula

**Files:**
- Create: `tools/tests/test_clusters_aula06.py`

**Interfaces:**
- Consumes: os cinco CSVs de `dados/`, com colunas `periodo,valor,unidade`.
- Produces: nada para outras tasks importarem. A fixture `base` e os auxiliares `_ler`, `_kmeans`, `_concordancia` e `_participacao_no_ano` ficam privados ao arquivo de teste. O notebook (Task 3) e o script de figuras (Task 2) reimplementam a mesma lógica de forma independente, de propósito: se as duas implementações divergirem, os números param de bater e o Step 4 da Task 3 pega.

- [ ] **Step 1: Escrever o arquivo de teste completo**

```python
"""Trava as conclusoes que a Aula 06 afirma sobre o agrupamento das cinco series.

A aula inteira e construida sobre um contraste: agrupar os trimestres pelos
NIVEIS de producao devolve epocas, e agrupar pela PARTICIPACAO de cada
trimestre no total do proprio ano devolve o trimestre do calendario. Se algum
CSV for regerado com dado novo do SIDRA e esse contraste desaparecer, o deck, o
material, o notebook e as notas do professor passam a ensinar algo que o dado
nao sustenta mais.

Como em `test_modelo_aula05.py`, o que se trava aqui sao as conclusoes:

1. agrupar os niveis nao recupera o trimestre (fica no acaso) e produz blocos
   contiguos no tempo;
2. agrupar a participacao no ano recupera o trimestre em mais de 95% das linhas;
3. a silhueta premia o agrupamento MENOS util, que e o achado central da aula;
4. as duas linhas que escapam do ato 2 sao as duas de 2008;
5. o leite tem pico no T4 enquanto as carnes tem pico no T3;
6. a amplitude sazonal do leite e mais que o triplo da do frango.

A ordem entre frango (1,08 p.p.) e ovos (1,06 p.p.) NAO e travada: os dois
estao a 0,02 ponto percentual um do outro, e afirmar qual e o menor seria ler
ruido como achado.

Precisa de numpy e scikit-learn, que estao no requirements-ci.txt. Sem eles o
arquivo inteiro se pula, e o CI continua cobrindo.

Cada assercao foi vista falhando ao menos uma vez, contra versoes
propositalmente quebradas do pipeline: remover a padronizacao do ato 2,
normalizar pelo total da serie inteira em vez de por ano, e usar K=3.
"""
import csv
import os

import pytest

np = pytest.importorskip("numpy")
sk_cluster = pytest.importorskip("sklearn.cluster")
sk_pre = pytest.importorskip("sklearn.preprocessing")
sk_met = pytest.importorskip("sklearn.metrics")

KMeans = sk_cluster.KMeans
StandardScaler = sk_pre.StandardScaler
silhouette_score = sk_met.silhouette_score

RAIZ = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
DADOS = os.path.join(RAIZ, "dados")
SERIES = [
    "abate_bovinos",
    "abate_suinos",
    "abate_frangos",
    "producao_ovos",
    "producao_leite",
]
K = 4
SEMENTE = 42


def _ler(nome):
    caminho = os.path.join(DADOS, nome + ".csv")
    with open(caminho, encoding="utf-8") as f:
        return {linha["periodo"]: float(linha["valor"]) for linha in csv.DictReader(f)}


@pytest.fixture(scope="module")
def base():
    """Interseccao das cinco series por periodo, ordenada no tempo.

    Interseccao, e nao uniao: `producao_ovos` comeca em 1987-T1 e as outras
    quatro em 1997-T1. E a mesma regra de juncao que a Aula 04 usou.
    """
    colunas = {s: _ler(s) for s in SERIES}
    periodos = sorted(set.intersection(*[set(c) for c in colunas.values()]))
    matriz = np.array([[colunas[s][p] for s in SERIES] for p in periodos])
    anos = np.array([int(p[:4]) for p in periodos])
    tris = np.array([int(p[-1]) for p in periodos])
    return {"periodos": periodos, "X": matriz, "anos": anos, "tris": tris}


def _kmeans(matriz):
    padronizada = StandardScaler().fit_transform(matriz)
    modelo = KMeans(n_clusters=K, n_init=50, random_state=SEMENTE).fit(padronizada)
    return modelo.labels_, silhouette_score(padronizada, modelo.labels_)


def _concordancia(rotulos, tris):
    """Fracao das linhas cobertas pelo trimestre majoritario de cada cluster."""
    acertos = 0
    for c in set(rotulos):
        do_cluster = tris[rotulos == c]
        acertos += max((do_cluster == t).sum() for t in (1, 2, 3, 4))
    return acertos / len(tris)


def _participacao_no_ano(b):
    """Cada valor vira a fracao que ele representa no total do proprio ano.

    So entram anos com os quatro trimestres medidos: 2026 tem apenas o T1, e
    incluir um ano incompleto faria o unico trimestre dele valer 100% do ano.
    """
    completos = {a for a in set(b["anos"].tolist()) if (b["anos"] == a).sum() == 4}
    mascara = np.array([a in completos for a in b["anos"]])
    X = b["X"][mascara]
    anos = b["anos"][mascara]
    saida = np.empty_like(X)
    for a in completos:
        linhas = anos == a
        saida[linhas] = X[linhas] / X[linhas].sum(axis=0)
    return saida, anos, b["tris"][mascara], [p for p, m in zip(b["periodos"], mascara) if m]


def test_base_tem_117_trimestres_de_1997_a_2026(base):
    assert len(base["periodos"]) == 117
    assert base["periodos"][0] == "1997-T1"
    assert base["periodos"][-1] == "2026-T1"


def test_agrupar_niveis_nao_recupera_o_trimestre(base):
    """Ato 1. A tendencia domina a variancia, e o resultado fica no acaso.

    Com quatro grupos, o acaso e 25%. Medido: 26,5%.
    """
    rotulos, _ = _kmeans(base["X"])
    assert _concordancia(rotulos, base["tris"]) < 0.35


def test_agrupar_niveis_devolve_blocos_contiguos_no_tempo(base):
    """Ato 1. Cada cluster e um intervalo fechado da linha do tempo.

    A base esta ordenada, entao um cluster contiguo ocupa posicoes consecutivas.
    Tolera uma unica troca na fronteira, que e onde 2019-T3 e 2020-T2 se
    encavalam.
    """
    rotulos, _ = _kmeans(base["X"])
    for c in set(rotulos):
        posicoes = np.where(rotulos == c)[0]
        vao = posicoes.max() - posicoes.min() + 1
        assert vao - len(posicoes) <= 2, (c, vao, len(posicoes))


def test_agrupar_participacao_no_ano_recupera_o_trimestre(base):
    """Ato 2. Removida a tendencia, o algoritmo redescobre o calendario.

    Medido: 114 de 116 linhas, 98,3%.
    """
    X, _, tris, periodos = _participacao_no_ano(base)
    assert len(periodos) == 116
    rotulos, _ = _kmeans(X)
    assert _concordancia(rotulos, tris) > 0.95


def test_a_silhueta_premia_o_agrupamento_menos_util(base):
    """O achado central da aula.

    Ato 1 tem silhueta 0,4795 e nao serve para nada no case. Ato 2 tem 0,2853 e
    responde a pergunta. A metrica mede separacao, e separacao nao e utilidade.
    """
    _, silhueta_niveis = _kmeans(base["X"])
    X, _, _, _ = _participacao_no_ano(base)
    _, silhueta_participacao = _kmeans(X)
    assert silhueta_niveis > silhueta_participacao
    assert 0.40 < silhueta_niveis < 0.55
    assert 0.22 < silhueta_participacao < 0.35


def test_as_duas_excecoes_do_ato_2_sao_de_2008(base):
    """As unicas duas linhas que caem fora do proprio trimestre sao de 2008."""
    X, _, tris, periodos = _participacao_no_ano(base)
    rotulos, _ = _kmeans(X)
    majoritario = {}
    for c in set(rotulos):
        do_cluster = tris[rotulos == c]
        majoritario[c] = max((1, 2, 3, 4), key=lambda t: (do_cluster == t).sum())
    fora = [p for p, r, t in zip(periodos, rotulos, tris) if majoritario[r] != t]
    assert len(fora) == 2
    assert all(p.startswith("2008") for p in fora), fora


def test_leite_tem_pico_no_t4_e_as_carnes_no_t3(base):
    """O pico do leite esta um trimestre a frente do das carnes.

    E o achado que serve ao Modelo 2 do TAPI, que converte producao em demanda
    de racao.
    """
    X, _, tris, _ = _participacao_no_ano(base)
    medias = {t: X[tris == t].mean(axis=0) for t in (1, 2, 3, 4)}
    i_leite = SERIES.index("producao_leite")
    for nome in ("abate_bovinos", "abate_suinos", "abate_frangos"):
        i = SERIES.index(nome)
        assert max((1, 2, 3, 4), key=lambda t: medias[t][i]) == 3, nome
    assert max((1, 2, 3, 4), key=lambda t: medias[t][i_leite]) == 4


def test_leite_e_mais_de_tres_vezes_mais_sazonal_que_frango(base):
    """Amplitude medida por trimestre do calendario: leite 3,85 p.p. e frango 1,10 p.p.

    A ordem entre frango (1,10) e ovos (1,08) nao entra: 0,02 ponto percentual
    de diferenca e ruido, e travar isso seria travar ruido.
    """
    X, _, tris, _ = _participacao_no_ano(base)
    medias = np.array([X[tris == t].mean(axis=0) for t in (1, 2, 3, 4)])
    amplitude = (medias.max(axis=0) - medias.min(axis=0)) * 100
    leite = amplitude[SERIES.index("producao_leite")]
    frango = amplitude[SERIES.index("abate_frangos")]
    assert leite > 3 * frango
    assert leite == max(amplitude)
```

- [ ] **Step 2: Rodar e ver os oito testes passarem**

Run: `/tmp/m03venv/bin/python -m pytest tools/tests/test_clusters_aula06.py -v`
Expected: 8 passed. Se algum falhar, o número medido mudou e a spec precisa ser corrigida antes de qualquer artefato ser escrito.

- [ ] **Step 3: Provar que os testes detectam pipeline quebrado**

Rodar as três mutações e confirmar que cada uma derruba pelo menos um teste. Aplicar, rodar, reverter:

1. Em `_participacao_no_ano`, trocar `X[linhas].sum(axis=0)` por `X.sum(axis=0)` (normalizar pela série inteira em vez de por ano). Deve derrubar `test_agrupar_participacao_no_ano_recupera_o_trimestre`.
2. Em `_kmeans`, remover o `StandardScaler` e passar `matriz` direto ao `KMeans`. Deve derrubar ao menos um teste.
3. Trocar `K = 4` por `K = 3`. Deve derrubar `test_as_duas_excecoes_do_ato_2_sao_de_2008`.

Registrar o resultado das três no docstring do arquivo se algum comportamento divergir do previsto acima.

- [ ] **Step 4: Rodar a suíte inteira, para garantir que nada regrediu**

Run: `/tmp/m03venv/bin/python -m pytest tools/tests/ -v`
Expected: todos passam.

- [ ] **Step 5: Commit**

```bash
git add tools/tests/test_clusters_aula06.py
git commit -m "test(aula06): trava o contraste entre agrupar niveis e agrupar participacao

Agrupar as cinco series em nivel devolve epocas (concordancia de 26,5% com o
trimestre, o acaso) e agrupar a participacao de cada trimestre no total do ano
recupera o calendario (98,3%). A silhueta premia o primeiro (0,4795 contra
0,2853), que e o achado central da Aula 06.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 2: Figuras da aula

**Files:**
- Create: `tools/graficos_aula06.py`
- Create (gerado): `assets/img/aula06-epocas-vs-trimestres.png`
- Create (gerado): `assets/img/aula06-perfil-sazonal.png`

**Interfaces:**
- Consumes: os cinco CSVs de `dados/`, com a mesma lógica de junção e de participação no ano da Task 1.
- Produces: dois PNG em `assets/img/`, citados pelo deck (Task 4) e pelo material (Task 5).

- [ ] **Step 1: Escrever o script**

Seguir `tools/graficos_aula05.py` como modelo: docstring que explica qual achado cada figura sustenta, paleta roxo `#2e2640` como tinta, coral `#ff4545` como destaque, verde `#89cea5` como terceira série, cinza `#caced6` nos eixos, `dpi=160`, largura de 12 polegadas e `fontsize` a partir de 18 pontos.

Cor literal em `tools/*.py` é permitida: `check_brand.py` varre o acervo publicado (HTML e CSS), não os scripts geradores. Conferir isso rodando o validador ao fim da task.

Figura 1, `aula06-epocas-vs-trimestres.png`: dois painéis lado a lado, ambos com o eixo x sendo o tempo de 1997-T1 a 2026-T1 e a série `abate_frangos` ao fundo em cinza. No painel esquerdo, cada ponto colorido pelo cluster do ato 1, que produz quatro faixas contíguas. No painel direito, cada ponto colorido pelo cluster do ato 2, que produz o padrão que se repete a cada quatro trimestres. Título do painel esquerdo: "Agrupando os níveis: quatro épocas". Título do direito: "Agrupando a participação no ano: quatro trimestres".

Figura 2, `aula06-perfil-sazonal.png`: barras agrupadas, cinco séries no eixo x e quatro barras por série (T1 a T4), com o valor em participação no ano. A barra do T4 do leite recebe o destaque coral, porque é o pico deslocado. Linha horizontal tracejada em 25%, que é a participação de um trimestre sem sazonalidade nenhuma.

- [ ] **Step 2: Gerar as figuras**

Run: `/tmp/m03venv/bin/python tools/graficos_aula06.py`
Expected: os dois PNG aparecem em `assets/img/`.

- [ ] **Step 3: Conferir a legibilidade em projeção**

Abrir cada PNG e confirmar que nenhum rótulo fica abaixo de 18 pixels quando a figura é exibida a 900px de largura. A conta está no cabeçalho de `tools/graficos_aula05.py`: com 12 polegadas de largura e `dpi=160`, um rótulo de N pontos chega à tela com aproximadamente N pixels.

- [ ] **Step 4: Rodar o validador de marca**

Run: `python3 tools/check_brand.py`
Expected: passa.

- [ ] **Step 5: Commit**

```bash
git add tools/graficos_aula06.py assets/img/aula06-*.png
git commit -m "feat(aula06): duas figuras do contraste entre epocas e trimestres

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 3: Notebook

**Files:**
- Create: `notebooks/aula06.ipynb`

**Interfaces:**
- Consumes: os cinco CSVs de `dados/`.
- Produces: o notebook que a turma roda nos blocos das 10h50, 11h20 e 11h40.

**Restrição dura:** precisa rodar no Google Colab sem nenhuma instalação. Só pandas, numpy, matplotlib e scikit-learn, que o Colab já traz. O bloco das 10h50 existe justamente para as duplas cujo ambiente local não funciona, então o notebook começa com o badge do Colab, como os das aulas anteriores.

- [ ] **Step 1: Montar a estrutura de células**

Seis seções, na ordem dos blocos da aula:

1. **Retomada (bloco das 10h50).** Uma célula única que lê os cinco CSVs, monta a base analítica de 113 linhas com as defasagens da Aula 04, ajusta a regressão da Aula 05 e imprime o MAPE. O aluno roda e vê 1,60%. Nenhum exercício aqui: o objetivo é o ambiente funcionando.
2. **O que K-means faz.** Célula de texto com a ideia de agrupar sem rótulo, mais uma célula que mostra a distância euclidiana entre dois trimestres escolhidos.
3. **Ato 1, agrupar os níveis.** Célula com `StandardScaler` e `KMeans(n_clusters=4, n_init=50, random_state=42)` sobre as cinco colunas em nível, imprimindo o intervalo de cada cluster e a concordância com o trimestre. O aluno vê 26,5%.
4. **Ato 2, agrupar a participação no ano.** Célula que converte cada valor na fração do total do próprio ano, descartando 2026 por estar incompleto, e repete o K-means. O aluno vê 98,3%.
5. **Interpretação.** Tabela do perfil sazonal e as duas exceções de 2008.
6. **Desafio.** Duas perguntas sem resposta na célula, respondidas no material de apoio: por que descartar 2026 muda o resultado, e o que aconteceria com K=2.

- [ ] **Step 2: Executar o notebook fora da árvore de trabalho**

```bash
cp notebooks/aula06.ipynb /tmp/aula06-teste.ipynb
cd /tmp && /tmp/m03venv/bin/python -m jupyter nbconvert --to notebook --execute /tmp/aula06-teste.ipynb --output /tmp/aula06-saida.ipynb
```
Expected: executa sem erro. Se `nbconvert` não estiver no venv, instalar com `/tmp/m03venv/bin/pip install nbconvert ipykernel`.

- [ ] **Step 3: Confirmar que o arquivo versionado não tem saída de execução**

Run: `grep -c '"output_type"' notebooks/aula06.ipynb`
Expected: 0. O acervo versiona notebook sem saída de propósito, para não entregar o gabarito do desafio.

- [ ] **Step 4: Conferir que os números batem com a Task 1**

Comparar a saída de `/tmp/aula06-saida.ipynb` com os valores da seção "Números medidos" deste plano. Divergência aqui significa que o notebook implementou a lógica de forma diferente do teste, e a divergência precisa ser resolvida antes de seguir.

- [ ] **Step 5: Commit**

```bash
git add notebooks/aula06.ipynb
git commit -m "feat(aula06): notebook com a retomada da Aula 05 e os dois agrupamentos

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 4: Deck

**Files:**
- Create: `aulas/aula06.html`

**Interfaces:**
- Consumes: `assets/css/inteli-brand.css`, `assets/css/inteli-theme.css`, `assets/css/inteli-print.css`, `assets/js/inteli-quiz.js`, `assets/js/inteli-zoom.js`, `assets/js/inteli-print.js`, e as duas figuras da Task 2.
- Produces: o deck que o professor conduz. `index.html` (Task 8) passa a apontar para ele.

Referência de markup de cada classe: `aulas/_fixture-tema.html`. Estrutura de comentário de cabeçalho: `aulas/aula05.html`.

- [ ] **Step 1: Escrever o cabeçalho e a capa**

O comentário do `<head>` registra a data, o sprint, os blocos e os achados medidos, como em `aulas/aula05.html`. Precisa dizer explicitamente que esta aula reduz escopo em relação ao roteiro original e por quê, apontando para `docs/adrs/ADR-009`.

Capa: eyebrow "Módulo 03 IN · Aula 06 · 01/09/2026", h1 "Aprendizado Não Supervisionado parte I", h3 "Agrupar os trimestres sem rótulo, e o que o resultado revela sobre o dado do case".

- [ ] **Step 2: Escrever os 35 slides**

Ordem e conteúdo. Dez dos 35 slides são condicionais (os módulos de revisão, 11 a 20), e o professor visita dois ou três deles. A coluna "rodapé" segue a regra da seção Global Constraints.

| # | Classe | Título | Rodapé |
|---|---|---|---|
| 1 | cover-slide | (capa) | sem |
| 2 | content-slide | Agenda do encontro | 2 |
| 3 | section-slide | Diagnóstico | sem |
| 4-9 | quiz-slide | as seis perguntas da seção 5.1 da spec, uma por slide | 3 a 8 |
| 10 | content-slide | Índice da revisão: onde cada tópico está | 9 |
| 11-20 | content-slide | cinco módulos de revisão, dois slides cada | 10 a 19 |
| 21 | section-slide | Retomada com as mãos | sem |
| 22 | exercise-slide | Reproduzir o MAPE de 1,60% da Aula 05 | 20 |
| 23 | section-slide | Agrupar sem rótulo | sem |
| 24 | content-slide | K-means agrupa por distância, e distância depende de escala | 21 |
| 25 | exercise-slide | Ato 1: agrupar as cinco séries em nível, com K=4 | 22 |
| 26 | content-slide | Agrupar os níveis devolve quatro épocas, e 26,5% de acerto no trimestre é o acaso | 23 |
| 27 | exercise-slide | Ato 2: agrupar a participação de cada trimestre no ano | 24 |
| 28 | content-slide | Removida a tendência, K-means recupera o trimestre em 114 das 116 linhas | 25 |
| 29 | content-slide | A silhueta cai de 0,4795 para 0,2853 no agrupamento que responde à pergunta | 26 |
| 30 | content-slide | O leite tem pico no T4 e as carnes no T3 | 27 |
| 31 | content-slide | As duas linhas que escapam do padrão são de 2008 | 28 |
| 32 | section-slide | ART.6 | sem |
| 33 | content-slide | O que a ART.6 pede e o que as Aulas 01 a 06 já produziram | 29 |
| 34 | content-slide | Referências | 30 |
| 35 | end-slide | Até 04/09 | sem |

O slide 10 é o índice da revisão dirigida: uma tabela com a pergunta do quiz, o tópico e o número do slide de destino, para o professor navegar digitando o número no Reveal.

Slide 29 é o slide mais importante da aula. O título declara o achado e o corpo mostra os dois números lado a lado, com a conclusão de que separação e utilidade são coisas medidas de forma diferente. Sem paralelismo.

- [ ] **Step 3: Rodar o validador de layout**

Run: `python3 tools/check_slides.py aulas/aula06.html`
Expected: nenhum slide com problema.

- [ ] **Step 4: Medir a folga vertical de cada slide**

```bash
python3 tools/check_slides.py aulas/aula06.html --shots /tmp/shots06
```

Além disso, conferir a folga mínima. Qualquer slide com menos de 80px de folga vertical no macOS é candidato a reprovar no Ubuntu do CI e precisa de corte de texto, não de redução de figura.

- [ ] **Step 5: Rodar marca e links**

Run: `python3 tools/check_brand.py && python3 tools/check_links.py`
Expected: os dois passam.

- [ ] **Step 6: Conferir o estado pós-interação, que os validadores não pegam**

Servir o deck (`python3 -m http.server 8000`), abrir `http://localhost:8000/aulas/aula06.html`, responder cada um dos seis quizzes e confirmar que a classe de estado muda a tela, não só o DOM. Navegar por número até cada módulo de revisão. Exportar o PDF de verdade e conferir que os `section-slide` não ficam colados no topo.

- [ ] **Step 7: Commit**

```bash
git add aulas/aula06.html
git commit -m "feat(aula06): deck da consolidacao das Aulas 01 a 05 com K-means

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 5: Material de apoio

**Files:**
- Create: `materiais/aula06.html`

**Interfaces:**
- Consumes: `assets/css/inteli-material.css` e as duas figuras da Task 2.
- Produces: a leitura que sustenta o que o deck só aponta.

Referência de estrutura: `materiais/aula05.html`.

- [ ] **Step 1: Escrever o material**

Cobre, além do que o deck mostra: a resposta às duas perguntas do desafio do notebook (por que 2026 sai da conta e o que K=2 produz), o motivo de 2008 ser o ano das duas exceções, a relação entre a amplitude sazonal pequena do frango e os coeficientes de `sen` e `cos` da Aula 05, e a divergência entre esta aula e o que `PLANEJAMENTO_AULA_A_AULA.md` prometia, tratada abertamente como a Aula 05 tratou a divergência com a Aula 04.

Sobre a amplitude, escrever a conclusão que a medição sustenta: leite tem mais que o triplo da amplitude do frango, e frango e ovos estão empatados no piso a 0,02 ponto percentual um do outro.

- [ ] **Step 2: Rodar marca e links**

Run: `python3 tools/check_brand.py && python3 tools/check_links.py`
Expected: os dois passam.

- [ ] **Step 3: Conferir o layout abaixo de 900px**

Abrir no navegador e reduzir a janela: a TOC lateral sai do layout e o grid volta para uma coluna sem deixar buraco.

- [ ] **Step 4: Commit**

```bash
git add materiais/aula06.html
git commit -m "docs(aula06): material de apoio

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 6: Referências

**Files:**
- Create: `referencias/aula06.html`

**Interfaces:**
- Consumes: `assets/css/inteli-material.css`.
- Produces: a página numerada que o deck cita no slide 34.

Referência de estrutura: `referencias/aula05.html`.

- [ ] **Step 1: Escrever a página**

Seção 1, os cinco autoestudos desta aula, copiados com título exato de `docs/autoestudos-por-semana.md`, Semana 05:

- Determinando K: Elbow Plot
- Determinando K: Silhouette Analysis
- Introdução ao aprendizado não supervisionado (IBM)
- K-means
- Opcional: PCA

Registrar na própria página que Elbow e Silhouette foram lidos nesta semana e que o método de escolha de K é tratado na Aula 08, com o motivo em `docs/adrs/ADR-009`. Sem isso, a página promete uma cobertura que a aula não dá.

Seção 2, leitura complementar do professor: manter a estrutura fixa que o acervo replica, listando as fontes efetivamente citadas no deck e no material (as cinco tabelas do SIDRA, a documentação de `KMeans`, `StandardScaler` e `silhouette_score` do scikit-learn, e Rousseeuw 1987 para a silhueta).

- [ ] **Step 2: Conferir título por título contra a fonte**

Run: `grep -A 8 "Semana 05" docs/autoestudos-por-semana.md`
Comparar caractere a caractere com o que está na página. Autoestudo que não está na fonte não entra.

- [ ] **Step 3: Rodar marca e links**

Run: `python3 tools/check_brand.py && python3 tools/check_links.py`
Expected: os dois passam.

- [ ] **Step 4: Commit**

```bash
git add referencias/aula06.html
git commit -m "docs(aula06): pagina de referencias com os cinco autoestudos da Semana 05

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 7: Notas do professor

**Files:**
- Create: `docs/notas-do-professor/aula06.md`

**Interfaces:**
- Consumes: o roteiro da seção 5 da spec.
- Produces: o material de condução do encontro.

Cabeçalho na forma nova, como `docs/notas-do-professor/aula05.md`, que já nasceu em conformidade com as diretivas de tom. Não copiar o cabeçalho das Aulas 01 a 04, que usa construção proibida e está listado como achado aberto em `docs/ANDAMENTO.md`.

- [ ] **Step 1: Escrever as notas**

Precisa conter, além das perguntas por bloco com resposta esperada e o erro que cada uma revela:

1. **A ordem de prioridade da revisão dirigida.** Se o quiz reprovar os seis itens, 20 minutos não cobrem tudo. Prioridade: perguntas 4 e 5 primeiro (corte temporal e baseline), porque são pré-requisito da Aula 07. Depois a 3 (as 113 linhas). A 6 (condicionamento numérico) é a última, e o material de apoio da Aula 05 já a cobre por escrito.
2. **A ordem de corte se o tempo apertar.** O primeiro a ceder é o bloco de ART.6 das 11h50, com a ressalva de que a amarração com a entrega foi um dos quatro problemas relatados, então cortá-lo tem custo. O último a cair é o par ato 1 e ato 2, que é a aula.
3. **O aviso sobre a divergência com o planejamento.** O roteiro original prometia perfis de dieta e o dado não entrega isso. Assumir em sala, como a Aula 05 assumiu a divergência com a Aula 04.
4. **O checkpoint de abertura.** Quais duplas conseguiram rodar a base analítica da Aula 04. Quem não conseguiu não acompanha o bloco das 10h50 e precisa ir para o Colab pelo badge do notebook.
5. **Onde está o peso da aula.** O slide 29, da silhueta que premia o agrupamento menos útil. Um aluno que saia sabendo rodar `KMeans` e sem saber que a métrica não mede utilidade não aprendeu o que a aula ensina.

- [ ] **Step 2: Conferir o tom**

Reler procurando paralelismo negativo, antítese simétrica, escalada com dois-pontos, título de listicle, travessão em dash e emoji. Corrigir o que aparecer.

- [ ] **Step 3: Commit**

```bash
git add docs/notas-do-professor/aula06.md
git commit -m "docs(aula06): notas do professor com a ordem de prioridade da revisao

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 8: ADR, documentos de planejamento e portal

**Files:**
- Create: `docs/adrs/ADR-009-reducao-de-escopo-da-aula-06.md`
- Modify: `PLANEJAMENTO_AULA_A_AULA.md` (bloco da Aula 06, linha de daily da Aula 07, bloco da Aula 08)
- Modify: `PLANO_DE_ENSINO.md` (tabela da seção 3, linhas 06 e 08; tabela da seção 6, linhas 07 e 08)
- Modify: `index.html` (card da Aula 06)
- Modify: `docs/ANDAMENTO.md`

- [ ] **Step 1: Escrever a ADR-009**

Seguir a estrutura mínima das ADRs existentes: Data, Status, Decisores, Contexto, Decisão, Motivações, Riscos conhecidos com mitigação, Consequências positivas e negativas, ADRs relacionadas.

Decisão em uma frase: a Aula 06 fixa K em 4, move Elbow Plot e Silhouette Analysis para a Aula 08, e usa metade do encontro para retomar as Aulas 01 a 05.

Motivações: a avaliação do professor sobre o nível da turma nos quatro eixos, e a medição que mostra que o agrupamento prometido pelo roteiro original não existe no dado.

Riscos: a Aula 08 fica sobrecarregada, e os alunos leram Elbow e Silhouette no autoestudo de 01/09 e só verão o método em 10/09.

ADRs relacionadas: ADR-005 (quatro artefatos por aula), ADR-007 (base analítica a partir das cinco séries), ADR-008 (corte temporal e baseline).

- [ ] **Step 2: Substituir o roteiro da Aula 06 em `PLANEJAMENTO_AULA_A_AULA.md`**

O bloco atual das linhas 374 a 410 vira o roteiro de sete blocos da seção 5 da spec. Manter o formato de todos os outros blocos do arquivo: autoestudo, daily, e as janelas com horário.

- [ ] **Step 3: Ajustar a Aula 07 e a Aula 08 no mesmo arquivo**

Na Aula 07, a linha do daily diz "Cada dupla relata o K escolhido na Aula 06 para os próprios clusters". K não é mais escolhido pela dupla, é fixo em 4. Trocar por um relato dos perfis encontrados. A linha de resgate das 10h15 continua válida, porque os perfis existem.

Na Aula 08, acrescentar Elbow Plot e Silhouette Analysis ao escopo, com a nota de que vieram da Aula 06 e de que o corte compensatório será decidido quando a aula for construída.

- [ ] **Step 4: Ajustar `PLANO_DE_ENSINO.md`**

Seção 3, linha 06: a camada da espiral deixa de ser "clusterização de perfis de dieta e de meses" e passa a ser a retomada mais o agrupamento dos trimestres. Linha 08: acrescentar a escolha de K.

Seção 6, linha 07: o que a Aula 06 deixa pronto. Linha 08: idem.

- [ ] **Step 5: Habilitar o card da Aula 06 no portal**

Em `index.html`, trocar `aria-disabled="true"` pelos quatro links reais do card da Aula 06, e atualizar o resumo para descrever a aula que existe agora.

- [ ] **Step 6: Atualizar `docs/ANDAMENTO.md`**

A Aula 06 sai de "não iniciado" e entra em "Pronto", com os achados medidos. A dívida herdada pela Aula 08 fica escrita numa seção própria, para quem construir a Aula 08 não descobrir tarde.

- [ ] **Step 7: Rodar links e marca**

Run: `python3 tools/check_links.py && python3 tools/check_brand.py`
Expected: os dois passam, agora com os quatro artefatos da Aula 06 existindo.

- [ ] **Step 8: Commit**

```bash
git add docs/adrs/ADR-009-reducao-de-escopo-da-aula-06.md PLANEJAMENTO_AULA_A_AULA.md PLANO_DE_ENSINO.md index.html docs/ANDAMENTO.md
git commit -m "docs(aula06): ADR-009, roteiro novo e portal

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 9: Revisão final e validação completa

**Files:**
- Modify: o que a revisão apontar.

- [ ] **Step 1: Rodar a bateria inteira**

```bash
python3 tools/check_brand.py
python3 tools/check_links.py
python3 tools/check_slides.py
/tmp/m03venv/bin/python -m pytest tools/tests/ -v
```
Expected: os quatro passam.

- [ ] **Step 2: Confirmar que a validação não alterou arquivo versionado**

Run: `git status --porcelain`
Expected: vazio. O `validate.yml` tem um passo que reprova se a validação mexer em arquivo versionado.

- [ ] **Step 3: Passar o agente `revisor-slides` no deck**

O agente cobre o que os validadores não pegam: especificidade CSS de estado, estado pós-interação, impressão de verdade, fonte pequena demais para projeção, numeração de rodapé e profundidade pedagógica.

- [ ] **Step 4: Corrigir o que a revisão apontar e revalidar**

Repetir o Step 1 depois de cada correção.

- [ ] **Step 5: Commit e push**

```bash
git add -A
git commit -m "fix(aula06): ajustes da revisao do deck

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
git push
```

- [ ] **Step 6: Conferir o CI no Ubuntu**

Run: `gh run watch`
Expected: verde. Se `check_slides.py` reprovar no Ubuntu com o deck passando no macOS, a causa é métrica de fonte, e a correção é encurtar texto, não reduzir figura. A armadilha está documentada em `CLAUDE.md`.

---

## Self-review

**Cobertura da spec.** Seção 1.1 (medição) fica na Task 1. Seções 2 e 5 (objetivos e roteiro) ficam nas Tasks 4 e 7. Seção 4.1 (Elbow para a Aula 08) fica na Task 8. Seção 4.2 (slides planos) está nas Global Constraints e na Task 4. Seção 4.3 (bloco de ART.6) fica nas Tasks 4 e 7. Seção 6 (artefatos) cobre as Tasks 2 a 7. Seção 6.1 (documentos) fica na Task 8. Seção 7 (riscos) fica na Task 7, item 1, e na Task 9, Step 6. Seção 8 (critérios de aceite) fica na Task 9.

**Consistência de nomes.** `_kmeans`, `_concordancia` e `_participacao_no_ano` aparecem com a mesma assinatura em todos os testes da Task 1. Os nomes de arquivo de figura são idênticos nas Tasks 2, 4 e 5.

**Dependência entre tasks.** A Task 1 precisa vir primeiro: ela é a fonte de todos os números que as Tasks 2 a 7 citam. As Tasks 5, 6 e 7 podem correr em paralelo depois da Task 4. A Task 8 precisa das Tasks 4 a 7 para os links do portal resolverem. A Task 9 fecha.
